<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Gestionnarire de bibliothèque{% endblock %}</title>
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
        <link rel="stylesheet" type="text/css" href="{{ asset( 'bundles/biblio/css/bootstrap.css' ) }}" />
        <link rel="stylesheet" type="text/css" href="{{ asset( 'bundles/biblio/css/font-awesome.css' ) }}" />
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        <div class="container">
            <div class="row">
                {{ render( controller( 'BiblioBundle:Home:menu' ) ) }}
                {% block body %}{% endblock %}
            </div>
        </div>
        {% block javascripts %}{% endblock %}
    </body>
</html>
